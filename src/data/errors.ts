export const errorStates = [
  { error: "Login failure", origin: "SAML / BoxyHQ / Auth0", uiAction: "next-auth redirects to error page; session not created" },
  { error: "OAuth2 token fetch failure", origin: "BFF → auth-api", uiAction: "BFF returns 401; UI redirects to login" },
  { error: "generateSession error", origin: "AppSync → session-api", uiAction: "UI shows generic error; user prompted to retry" },
  { error: "generateSession DynamoDB timeout", origin: "session-api → DynamoDB", uiAction: "session-api returns 503; AppSync surfaces error to UI" },
  { error: "subscriptionQualification — ineligible", origin: "reseller-service → catalog-api", uiAction: 'UI shows "not eligible"; purchase options hidden' },
  { error: "subscriptionQualification timeout", origin: "reseller-service", uiAction: "AppSync returns error; UI shows loading error with retry" },
  { error: "submitSubscription — merchant rejected", origin: "reseller-service → merchant-api-*", uiAction: "Provider-specific error message shown" },
  { error: "submitSubscription — DB write failure", origin: "reseller-service → PostgreSQL", uiAction: "reseller-service rolls back; AppSync returns 500; UI shows error" },
  { error: "submitSubscription — Kafka failure", origin: "reseller-service → Kafka", uiAction: "Logged to DLQ; order still created; consumers retry" },
  { error: "activateSubscription error", origin: "reseller-service → merchant-api-*", uiAction: '"Activation failed" shown; user can retry or contact support' },
  { error: "Subscription list load failure", origin: "BFF → subscriptions-aggregator-api", uiAction: "Empty state with refresh button" },
  { error: "CIAM /bans failure", origin: "BFF → CIAM service", uiAction: "BFF returns 500; account selector cannot load" },
  { error: "Session expired (DynamoDB TTL)", origin: "session-api", uiAction: "AppSync returns session-not-found; UI restarts from generateSession" },
  { error: "Feature flag service unreachable", origin: "Unleash / Go Feature Flags", uiAction: "All flags default to OFF; UI continues with base feature set" },
  { error: "BFF session cookie expired", origin: "next-auth", uiAction: "BFF redirects to login; in-progress flow is lost" },
  { error: "BFF token expiry mid-flow", origin: "BFF → auth-api", uiAction: "BFF silently refreshes; if refresh fails → redirect to login" },
];

export const timingRows = [
  { call: "UI → BFF /api/protected/graphql", mode: "Sync", latency: "< 50 ms", notes: "BFF adds OAuth2 token then forwards" },
  { call: "UI → BFF /api/protected/subscriptions", mode: "Sync", latency: "< 50 ms", notes: "BFF forwards to aggregator-api" },
  { call: "BFF → AppSync", mode: "Sync", latency: "< 100 ms", notes: "Single GraphQL mutation" },
  { call: "AppSync → session-api (generateSession)", mode: "Sync", latency: "100–300 ms", notes: "DynamoDB write; returns sessionId" },
  { call: "AppSync → household-api", mode: "Sync", latency: "200–500 ms", notes: "CPM round-trip" },
  { call: "AppSync → reseller-service (qualify)", mode: "Sync", latency: "300–800 ms", notes: "Calls catalog-api (Redis); eligibility rules" },
  { call: "AppSync → reseller-service (submit)", mode: "Sync", latency: "500 ms–2 s", notes: "DB write + merchant-api-* call" },
  { call: "reseller-service → merchant-api-*", mode: "Sync", latency: "300 ms–1.5 s", notes: "External provider round-trip" },
  { call: "reseller-service → Kafka publish", mode: "Async", latency: "< 10 ms (fire & forget)", notes: "Non-blocking; DLQ on failure" },
  { call: "AppSync → reseller-service (activate)", mode: "Sync", latency: "300 ms–1 s", notes: "Merchant provisioning call" },
  { call: "BFF → subscriptions-aggregator-api", mode: "Sync", latency: "200–600 ms", notes: "PostgreSQL + CPM reads" },
  { call: "Kafka → subscription-consumer", mode: "Async", latency: "eventual (< 5 s p99)", notes: "Dapr sidecar manages retries" },
  { call: "Kafka → promoredeem-consumer", mode: "Async", latency: "eventual (< 5 s p99)", notes: "DLQ after 3 failed attempts" },
];
